# Verdict guard (condition) — completed

> Condition: the council output must contain an explicit RECOMMENDATION or the pipeline stops rather than shipping mush.

Condition evaluated: TRUE (mode: contains, expression: "RECOMMENDATION"). Action: continue.
