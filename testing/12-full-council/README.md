# 12 · Full council step
manager+worker council (1 round / 0 revisions) through the deliberation engine via the pipeline. Verifies completion, output artifact, decision + worker output in the council store. Real NIM calls.
