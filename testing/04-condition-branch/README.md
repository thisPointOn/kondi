# 04 · Condition branching
Three pipelines: contains-match true→continue (downstream runs); false→stop (downstream skipped, pipeline completes); true→skip_next_stage (next layer skipped, following layer runs).
