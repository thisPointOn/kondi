# 01 · Linear chain
Text input + instructions → **analysis** step (manager-only decide; artifact must be the DECISION) → **agent** step summarizing it.
Verifies: input+instructions delivery, decision artifact extraction, step→step chaining. Uses real NIM calls.
