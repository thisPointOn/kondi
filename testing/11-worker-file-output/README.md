# 11 · Worker file output + {{file}}
Working directory set to this folder; worker saveOutput → executor writes the output file here; artifact.outputPath set; downstream {{file}} template resolves to the path.
