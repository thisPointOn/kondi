# 07 · Input sources
file / directory / url input sources with instructions.
Verifies: first step's prompt contains the [Input type: …] block, path/URL, read-with-tools instruction, the user instructions — and for url, the ACTUAL fetched body (example.com).
